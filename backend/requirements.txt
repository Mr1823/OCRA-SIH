# ORCA Backend — Python dependencies
# Install: pip install -r requirements.txt

# ── Web framework ──
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# ── Environment ──
python-dotenv>=1.0.0

# ── HTTP client (geocoding, live data) ──
httpx>=0.27.0

# ── Anthropic Claude SDK ──
anthropic>=0.125.0

# ── Groq SDK (primary LLM provider) ──
groq>=1.0.0

# ── Copernicus Marine Service (live chlorophyll-a) ──
copernicusmarine>=2.0.0

# ── Testing ──
pytest>=8.0.0
pytest-asyncio>=0.23.0

